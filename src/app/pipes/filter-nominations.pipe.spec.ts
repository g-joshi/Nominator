import { FilterNominationsPipe } from './filter-nominations.pipe';

describe('FilterNominationsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNominationsPipe();
    expect(pipe).toBeTruthy();
  });
});
